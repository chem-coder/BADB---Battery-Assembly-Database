# Copy to .env.local and fill in real values. Never commit .env.local.
#
# E2E / AI-assisted manual test account (normal app login, not auth bypass)
E2E_TEST_USERNAME=
E2E_TEST_PASSWORD=

# Optional: target a non-default BADB server for E2E scripts
# E2E_BASE_URL=http://localhost:3003
