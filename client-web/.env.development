# Пустой baseURL — запросы идут через Vite-прокси (/api → localhost:3003)
VITE_API_URL=

# Auth bypass — пропустить экран логина при разработке
# Убрать перед демо/выступлением!
VITE_AUTH_BYPASS=true
