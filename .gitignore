node_modules/
.env
.env.*
!.env.example
.e2e-session.json
*.db
*.log
*.tmp
~$*.xl*
.DS_Store
Thumbs.db
dist/
build/
.claude/
.local/
__pycache__/
*.pyc
uploads/
# Generated SQL from the cycling import scripts — regenerate via the .py tools,
# never commit (1.2 MB dump of the colleague Excel data).
scripts/cycling-ref-import/combined_import.sql
mempalace.yaml
mempalace.yaml.old
entities.json
entities.json.old
public-vue/
pg_dump workflow.txt
ql.local/

# Windows PC autobackups; may contain people/data/password hashes and sensitive data.
sql_backups/auto/

# Local full database backups; may contain people/data/password hashes.
sql_backups/
sql_backups/local_only/
sql_backups/*_full*.sql
sql_backups/*dump*.sql

# Python cache (one-off scripts/)
scripts/__pycache__/
**/__pycache__/

# Runtime upload directory — user-generated data, not source
uploads/

# macOS / Finder duplicates ("filename (2).ext") — accidental copies
* (2)
* (2).*
* (2) *
