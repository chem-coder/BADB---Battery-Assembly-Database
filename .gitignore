node_modules/
.env
.env.*
*.db
*.log
*.tmp
~$*.xl*
.DS_Store
Thumbs.db
dist/
build/
.claude/
.local/
mempalace.yaml
mempalace.yaml.old
entities.json
entities.json.old
public-vue/
pg_dump workflow.txt
ql.local/

# Windows PC autobackups; may contain people/data/password hashes and sensitive data.
sql_backups/auto/

# Local full database backups; may contain people/data/password hashes.
sql_backups/
sql_backups/local_only/
sql_backups/*_full*.sql
sql_backups/*dump*.sql

# Python cache (one-off scripts/)
scripts/__pycache__/
**/__pycache__/

# Runtime upload directory — user-generated data, not source
uploads/

# macOS / Finder duplicates ("filename (2).ext") — accidental copies
* (2)
* (2).*
* (2) *
